/*
1. document.getElementsByTagName();         // Lấy ra các thẻ HTML
2. document.getElementsByClassName();		// Lấy ra các thẻ class
3. document.getElementById();				// Lấy ra các thẻ ID
4. document.createElement("li");			// Tạo ra các thẻ HTML
5. document.querySelectorAll("p.class");	// Lấy các thẻ HTML chứa class hay id nào đó
6. .setAttribute("href", 'abc.com');		// Tạo ra các thuộc tính bên trong thẻ HTML

7. .children('selector');					// Lấy các thành phần con trong class hay id bố nào đó
8. .textContent('selector');				// Lấy tất cả nội dung
9. .appendChild('selector');				// Thêm các nội dung vào HTML
10. .remove('selector');					//Xoá bỏ

11. document.getElementById().value;       	// Lấy giá trị từ tag chứa ID ra


12. document.getElementById('idForm').reset();   //Reset toàn bộ nội dung trong form
13. .addEventListener('click',function)			// Thay thế onclick
14. .addEventListener('change',function)		// Hay dùng trong việc sắp xếp,lọc dữ liệu
15. .addEventListener('keyup',function)			// Hay dùng trong việc tự động như tự động tính toán, tự in ra kí tự đang gõ
16. <script src="https://unpkg.com/axios/dist/axios.min.js"></script>  //Nhúng axios vào trước script
17. document.write(content);                 //Hiển thị nội dung ra trình duyệt

*/